datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

generator client {
  provider = "prisma-client-js"
}

model TestEntity {
  id   String @unique @default(uuid())
  name String

  @@map("test_entity")
}
